name: zetra_id
description: Zetra ID management.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    package_info_plus: ^8.1.1

  cupertino_icons: ^1.0.2
  http: ^1.2.0
  shared_preferences: ^2.2.2
  supabase_flutter: ^2.3.4
  url_launcher: ^6.3.1
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.3.0
  flutter_local_notifications: ^17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  flutter_plugin_android_lifecycle: ^2.0.24

flutter:
  uses-material-design: true
